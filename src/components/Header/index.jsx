import React from 'react';
import './styles.scss';
import PropTypes from 'prop-types';

Header.propTypes = {
    
};

function Header(props) {
    return (
        <div>
            Header
        </div>
    );
}

export default Header;